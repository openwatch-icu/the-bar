{
  "name": "the-bar",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "tauri": "CI=true CARGO_TARGET_DIR=./target tauri",
    "dev": "CARGO_TARGET_DIR=./target tauri dev",
    "build": "CI=true CARGO_TARGET_DIR=./target tauri build",
    "build:desktop": "VITE_WS_URL= VITE_INSTANCE_SLUG= CI=true CARGO_TARGET_DIR=./target tauri build",
    "build:appimage": "NO_STRIP=1 CI=true CARGO_TARGET_DIR=./target tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/plugin-notification": "^2.3.3"
  }
}
